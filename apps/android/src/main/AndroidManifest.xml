<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Only so the app can offer to clear the screenshots it just uploaded. It
         asks at the moment the offer is accepted, never on launch. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <application
        android:name="io.missiongo.android.MissionGoApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.MissionGo">
        <!--
             androidx.work arrives through the feedback SDK, and its startup
             provider runs before Application.onCreate to bring up WorkManager and
             open its Room database. This app never uses WorkManager: it reaches
             the SDK only through MissionGo.openFeedback, which starts an Activity.
             Only enqueueFeedback, cancelQueuedFeedback and retryQueuedFeedback
             touch WorkManager, and nothing here calls them, so the work is paid
             on every cold start for a feature the app does not have.

             This removal applies to this app's merged manifest alone; a host app
             that integrates the SDK keeps its own initializer and its queued
             submissions. If this app ever does call one of those three, restore
             this provider or initialize WorkManager on demand; without it
             WorkManager.getInstance throws.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove" />

        <activity
            android:name="io.missiongo.android.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
