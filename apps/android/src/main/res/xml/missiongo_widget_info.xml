<?xml version="1.0" encoding="utf-8"?>
<!--
  2x2 by default, stretchable to 4x2 (AND-149). updatePeriodMillis is 0: the
  periodic refresh is WidgetRefreshWorker, which can require a network and
  whose 15 minutes is what the platform allows, where this attribute is held to
  30 minutes and wakes the device regardless.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/widget_missiongo"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:maxResizeWidth="320dp"
    android:maxResizeHeight="180dp"
    android:previewImage="@mipmap/ic_launcher"
    android:previewLayout="@layout/widget_missiongo"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
