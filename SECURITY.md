# Security Policy

## 报告安全问题

请不要用公开 Issue 报告漏洞、凭据或包含真实工作内容的问题。优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口，并提供最小复现步骤、影响范围和建议修复方向。

请勿上传生产数据库、附件、完整日志、账号密码、Token、Cookie、签名材料或真实部署配置。必要证据应先缩减到不会暴露用户数据的最小范围。

## 支持范围

当前仅维护默认分支的最新版本。项目仍处于早期内部试用阶段，尚未承诺长期支持版本或安全修复时限。

## 安全模型

- 单管理员、自托管；
- 网页会话使用 HTTP-only Cookie；
- AI 通过 OAuth 获取限时、只读、按产品约束的授权；
- SDK Token 仅允许在绑定产品范围内提交；
- 附件不作为静态公共目录暴露；
- MCP 不提供任意 SQL、任意字段修改或当前状态写入能力；
- 所有用户正文、日志、OCR 和附件都视为不可信输入。

详细约束见 [安全边界](docs/security-boundaries.md)。
